# Runtime model objects

Fitted model binaries are not included in this publication snapshot. The unchanged notebooks use the original `Models` directory convention. Saved outputs can be viewed without model files; reproducing frozen-object predictions requires matching model objects, input data and fitting provenance from the original experiment.
